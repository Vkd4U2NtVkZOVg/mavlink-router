[constants]
arch = 'mipsel'


[binaries]
c = arch + '-linux-gcc'
cpp = arch + '-linux-g++'
ar = arch + '-linux-gcc-ar'
strip = arch + '-linux-strip'
pkgconfig = 'pkg-config'

[host_machine]
system = 'linux'
cpu_family = 'mips'
cpu = 'mipsel'
endian = 'little'
